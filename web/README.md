# web/

Frontend placeholder for Flow Forge.

The Next.js console (aligned with Dify `web/`) will be initialized in a later OpenSpec change (`minimal-web`). This directory intentionally does **not** contain a runnable Next.js app yet.
