## 1. Next 工程骨架

- [x] 1.1 在 `web/` 初始化 Next.js（App Router + TypeScript），替换占位 README，并确认开发服务器可启动
- [x] 1.2 配置 API 代理或 CORS 方案（优先 Next rewrite），文档写明 `api` 与 `web` 同时启动方式

## 2. 联调页

- [x] 2.1 实现联调页：预填最小图 JSON、inputs 编辑、运行按钮，串起 create → run → events
- [x] 2.2 展示 run 终态、outputs 与事件列表；失败路径有可读错误
- [x] 2.3 补充根 README / `web/README` 跟跑步骤，并做一次本地手测或约定的静态检查（如 build）通过

## 3. 收尾

- [x] 3.1 确认示例图与后端校验一致，更新 `CONTEXT.md` 中 web 状态描述
